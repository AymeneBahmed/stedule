generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:../src/lib/dev.db"
}

model Task {
  id          Int     @id @default(autoincrement())
  name        String
  day         String
  time        String
  priority    String?
  description String?
}
